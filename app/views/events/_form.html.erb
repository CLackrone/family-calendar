<% if @event.errors.any? %>
 <div id="error_explanation">
  <h2><%= pluralize(@event.errors.count, "error") %> prohibited this from being saved:   </h2>

  <ul>
  <% @event.errors.full_messages.each do |msg| %>
    <li><%= msg %></li>
  <% end %>
  </ul>
 </div>
<% end %>


<%= form_for @event do |f| %>
  <%= f.label :name %>
  <%= f.text_field :name %><br>

  <%= f.label :time %>
  <%= f.text_field :time %><br>

  <%= f.label :location %>
  <%= f.text_field :location %><br>

  <%= f.submit %>

<% end %>


